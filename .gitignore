# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase CLI's local project-link cache (npx supabase login / gen types) —
# machine-specific, not part of the app.
supabase/.temp/

# Scratch SQL/scripts used for live verification during a session — never
# part of the app itself.
.scratch/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
