// PLACEHOLDER — regenerate this file for real once your Supabase project
// exists and has all four migrations applied. See docs/SETUP.md, "Generate
// types from the real schema":
//
//   npx supabase gen types typescript --project-id <your-project-ref> \
//     --schema public,app > src/lib/database.types.ts
//
// Until then this stub keeps the app compiling with `unknown` types instead
// of failing the build. Do not hand-write real types here — always generate
// them from the live schema, so they can never drift from the migrations in
// backend/migrations/. Re-run the command above any time you're unsure
// they're current; the schema itself should never change (see
// PROJECT_KNOWLEDGE.md — "THE DATABASE IS FIXED").

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
